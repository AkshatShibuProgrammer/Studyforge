<USER_REQUEST>
check this example ?
and analyze how it was working dont do any change
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Topic & Current Affairs Explorer</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- React & ReactDOM -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    
    <!-- Babel for JSX -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f1f5f9;
            color: #0f172a;
        }
        
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: #f1f5f9; border-radius: 4px; }
        ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #94a3b8; }

        .markdown-body p { margin-bottom: 0.75em; line-height: 1.5; }
        .markdown-body h1, .markdown-body h2, .markdown-body h3 { font-weight: 600; margin-top: 1em; margin-bottom: 0.5em; }
        .markdown-body h2 { font-size: 1.25em; border-bottom: 1px solid #e2e8f0; padding-bottom: 0.2em; }
        .markdown-body ul { list-style-type: disc; padding-left: 1.2em; margin-bottom: 1em; }
        .markdown-body strong { font-weight: 600; color: #1e293b; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        const Icons = {
            Search: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>,
            BookOpen: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path></svg>,
            Image: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><circle cx="8.5" cy="8.5" r="1.5"></circle><polyline points="21 15 16 10 5 21"></polyline></svg>,
            Loader: () => <svg className="animate-spin" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><line x1="12" y1="2" x2="12" y2="6"></line><line x1="12" y1="18" x2="12" y2="22"></line><line x1="4.93" y1="4.93" x2="7.76" y2="7.76"></line><line x1="16.24" y1="16.24" x2="19.07" y2="19.07"></line><line x1="2" y1="12" x2="6" y2="12"></line><line x1="18" y1="12" x2="22" y2="12"></line><line x1="4.93" y1="19.07" x2="7.76" y2="16.24"></line><line x1="16.24" y1="7.76" x2="19.07" y2="4.93"></line></svg>,
            Settings: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>,
            ArrowRight: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>,
            Check: () => <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
        };

        const MarkdownText = ({ text }) => {
            const renderHTML = (markdown) => {
                if (!markdown) return "";
                let html = markdown
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                    .replace(/\*(.*?)\*/g, '<em>$1</em>')
                    .replace(/### (.*?)\n/g, '<h3>$1</h3>\n')
                    .replace(/## (.*?)\n/g, '<h2>$1</h2>\n');

                const blocks = html.split('\n\n');
                return blocks.map((block, i) => {
                    if (block.trim().startsWith('* ') || block.trim().startsWith('- ')) {
                        const listItems = block.split('\n')
                            .filter(line => line.trim().length > 0)
                            .map(line => `<li>${line.replace(/^[\*\-]\s+/, '')}</li>`)
                            .join('');
                        return `<ul key="${i}">${listItems}</ul>`;
                    } else {
                        return `<p key="${i}">${block.replace(/\n/g, '<br/>')}</p>`;
                    }
                }).join('');
            };

            return <div className="markdown-body" dangerouslySetInnerHTML={{ __html: renderHTML(text) }} />;
        };

        // Interface with window.ai (Chrome's Built-in Gemini Nano)
        const runLocalAI = async (prompt, systemPrompt = "You are a helpful assistant.") => {
            try {
                if (window.ai && window.ai.languageModel) {
                    const capabilities = await window.ai.languageModel.capabilities();
                    if (capabilities.available !== "no") {
                        const session = await window.ai.languageModel.create({
                            systemPrompt: systemPrompt
                        });
                        const result = await session.prompt(prompt);
                        return result;
                    }
                }
            } catch (error) {
                console.warn("window.ai encounter an error. Using simulation fallback.", error);
            }

            // Fallback Simulation (if window.ai is disabled or unsupported in browser)
            await new Promise(resolve => setTimeout(resolve, 1200)); // Simulate thinking delay
            
            if (prompt.includes("search for the latest news")) {
                return `Search Query: "Latest updates and current affairs regarding ${prompt.split(':').pop().trim()}"`;
            } else if (prompt.includes("explain the core concepts")) {
                return `Explain the fundamentals, history, and core mechanisms of ${prompt.split(':').pop().trim()} in simple terms.`;
            } else if (prompt.includes("image generation prompt")) {
                return `A highly detailed, cinematic, and conceptual visual representation of ${prompt.split(':').pop().trim()}, 4k, trending on artstation.`;
            }

            return `**[Simulated AI Response]**\n\nYou asked: *${prompt}*\n\nSince Chrome's built-in \`window.ai\` (Gemini Nano) is not currently active on your browser, this is an offline simulated response to demonstrate the UI flow.`;
        };

        const App = () => {
            const [topic, setTopic] = useState('');
            const [step, setStep] = useState(1); // 1: Input, 2: Prompts, 3: Results
            const [loadingState, setLoadingState] = useState('');
            const [aiStatus, setAiStatus] = useState('Checking AI capabilities...');

            const [prompts, setPrompts] = useState({
                search: '',
                understand: '',
                image: ''
            });

            const [results, setResults] = useState({
                search: '',
                understand: '',
                image: ''
            });

            // Check if window.ai is natively available
            useEffect(() => {
                const checkStatus = async () => {
                    if (window.ai && window.ai.languageModel) {
                        try {
                            const cap = await window.ai.languageModel.capabilities();
                            if (cap.available !== "no") {
                                setAiStatus('Connected: Local Gemini Nano (window.ai)');
                                return;
                            }
                        } catch(e) {}
                    }
                    setAiStatus('Fallback Mode (window.ai not enabled in browser)');
                };
                checkStatus();
            }, []);

            const generatePrompts = async () => {
                if (!topic.trim()) return;
                
                setLoadingState('Crafting optimal prompts...');
                setStep(2); // Move to review step

                const pSearch = await runLocalAI(`Write a concise search prompt to find the latest news and current affairs regarding: ${topic}`, "You are an expert prompt engineer.");
                const pUnderstand = await runLocalAI(`Write a concise prompt asking to explain the core concepts and fundamentals of: ${topic}`, "You are an expert prompt engineer.");
                const pImage = await runLocalAI(`Write a highly descriptive image generation prompt (for Midjourney/DALL-E) to create a visual conceptual representation of: ${topic}`, "You are an expert prompt engineer.");

                setPrompts({
                    search: pSearch.replace(/"/g, ''),
                    understand: pUnderstand.replace(/"/g, ''),
                    image: pImage.replace(/"/g, '')
                });
                
                setLoadingState('');
            };

            const executePrompts = async () => {
                setLoadingState('Executing prompts using Inbuilt AI...');
                setStep(3); // Move to results step

                // 1 & 2. Run text generations concurrently using window.ai
                const [resSearch, resUnderstand] = await Promise.all([
                    runLocalAI(prompts.search, "You are a web search summarizer. Provide a concise summary of what one might find searching for this topic today."),
                    runLocalAI(prompts.understand, "You are an expert teacher. Explain the requested concept clearly and comprehensively.")
                ]);

                // 3. Image Generation Simulation 
                // (Since local window.ai cannot generate images natively, we use a placeholder API driven by the prompt)
                const safeTopic = encodeURIComponent(topic.substring(0, 20));
                const mockImageUrl = `https://placehold.co/800x400/1e293b/f8fafc?text=${safeTopic}+Concept\n(Simulated+Image)`;

                setResults({
                    search: resSearch,
                    understand: resUnderstand,
                    image: mockImageUrl
                });

                setLoadingState('');
            };

            const resetApp = () => {
                setTopic('');
                setStep(1);
                setPrompts({ search: '', understand: '', image: '' });
                setResults({ search: '', understand: '', image: '' });
            };

            const renderStep1 = () => (
                <div className="max-w-2xl mx-auto bg-white p-8 rounded-2xl shadow-sm border border-gray-100 text-center space-y-6">
                    <div className="w-16 h-16 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mx-auto mb-4">
                        <Icons.Search />
                    </div>
                    <h2 className="text-3xl font-bold text-gray-800">What would you like to explore?</h2>
                    <p className="text-gray-500">Enter a topic, and our Inbuilt AI will engineer specialized prompts to research current affairs, understand the concepts, and visualize it.</p>
                    
                    <div className="relative mt-6">
                        <input 
                            type="text" 
                            value={topic}
                            onChange={(e) => setTopic(e.target.value)}
                            onKeyDown={(e) => e.key === 'Enter' && topic && generatePrompts()}
                            placeholder="e.g. Quantum Computing, Climate Accords, Space Tourism..."
                            className="w-full px-6 py-4 text-lg bg-gray-50 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all"
                        />
                    </div>
                    
                    <button 
                        onClick={generatePrompts}
                        disabled={!topic.trim()}
                        className="w-full py-4 bg-slate-900 hover:bg-slate-800 text-white font-semibold rounded-xl transition-all disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2"
                    >
                        Generate Smart Prompts <Icons.ArrowRight />
                    </button>
                </div>
            );

            const renderStep2 = () => (
                <div className="max-w-4xl mx-auto space-y-6">
                    <div className="flex items-center justify-between mb-8">
                        <h2 className="text-2xl font-bold text-gray-800">Review & Edit Prompts</h2>
                        <button onClick={() => setStep(1)} className="text-gray-500 hover:text-gray-800 text-sm font-medium">← Back to Topic</button>
                    </div>

                    {loadingState ? (
                        <div className="p-12 text-center flex flex-col items-center justify-center text-gray-500 bg-white rounded-2xl border border-gray-100">
                            <Icons.Loader />
                            <p className="mt-4 font-medium">{loadingState}</p>
                        </div>
                    ) : (
                        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                            {/* Search Prompt */}
                            <div className="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col">
                                <div className="flex items-center gap-2 text-blue-600 font-semibold mb-3">
                                    <Icons.Search /> 1. Current Affairs
                                </div>
                                <p className="text-xs text-gray-500 mb-3">Instructs the AI to search for the latest news.</p>
                                <textarea 
                                    className="w-full flex-1 p-3 bg-gray-50 border border-gray-200 rounded-lg text-sm resize-none focus:ring-2 focus:ring-blue-500 focus:outline-none"
                                    rows="6"
                                    value={prompts.search}
                                    onChange={(e) => setPrompts({...prompts, search: e.target.value})}
                                />
                            </div>

                            {/* Understanding Prompt */}
                            <div className="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col">
                                <div className="flex items-center gap-2 text-emerald-600 font-semibold mb-3">
                                    <Icons.BookOpen /> 2. Deep Understanding
                                </div>
                                <p className="text-xs text-gray-500 mb-3">Instructs the AI to explain the fundamentals.</p>
                                <textarea 
                                    className="w-full flex-1 p-3 bg-gray-50 border border-gray-200 rounded-lg text-sm resize-none focus:ring-2 focus:ring-emerald-500 focus:outline-none"
                                    rows="6"
                                    value={prompts.understand}
                                    onChange={(e) => setPrompts({...prompts, understand: e.target.value})}
                                />
                            </div>

                            {/* Image Prompt */}
                            <div className="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col">
                                <div className="flex items-center gap-2 text-purple-600 font-semibold mb-3">
                                    <Icons.Image /> 3. Image Generation
                                </div>
                                <p className="text-xs text-gray-500 mb-3">Instructs the AI to create a visual representation.</p>
                                <textarea 
                                    className="w-full flex-1 p-3 bg-gray-50 border border-gray-200 rounded-lg text-sm resize-none focus:ring-2 focus:ring-purple-500 focus:outline-none"
                                    rows="6"
                                    value={prompts.image}
                                    onChange={(e) => setPrompts({...prompts, image: e.target.value})}
                                />
                            </div>
                        </div>
                    )}

                    {!loadingState && (
                        <div className="mt-8 flex justify-end">
                            <button 
                                onClick={executePrompts}
                                className="py-4 px-8 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl transition-all flex items-center gap-2 shadow-sm"
                            >
                                <Icons.Check /> Approve & Execute Prompts
                            </button>
                        </div>
                    )}
                </div>
            );

            const renderStep3 = () => (
                <div className="max-w-5xl mx-auto space-y-6">
                    <div className="flex items-center justify-between mb-8">
                        <div>
                            <h2 className="text-2xl font-bold text-gray-800">Exploration Results</h2>
                            <p className="text-gray-500 mt-1">Topic: <span className="font-semibold text-gray-800">{topic}</span></p>
                        </div>
                        <button onClick={resetApp} className="py-2 px-4 bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium rounded-lg transition-all text-sm">
                            Start New Exploration
                        </button>
                    </div>

                    {loadingState ? (
                        <div className="p-16 text-center flex flex-col items-center justify-center text-gray-500 bg-white rounded-2xl border border-gray-100">
                            <Icons.Loader />
                            <p className="mt-4 font-medium text-lg">{loadingState}</p>
                            <p className="text-sm mt-2 opacity-75">Processing concurrently via local models...</p>
                        </div>
                    ) : (
                        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
                            
                            <div className="space-y-8">
                                {/* Result: Current Affairs */}
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                                    <div className="flex items-center gap-2 text-blue-600 font-bold mb-4 text-lg border-b border-gray-100 pb-3">
                                        <Icons.Search /> Current Affairs & News
                                    </div>
                                    <div className="text-gray-700">
                                        <MarkdownText text={results.search} />
                                    </div>
                                </div>

                                {/* Result: Understanding */}
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                                    <div className="flex items-center gap-2 text-emerald-600 font-bold mb-4 text-lg border-b border-gray-100 pb-3">
                                        <Icons.BookOpen /> Concept Understanding
                                    </div>
                                    <div className="text-gray-700">
                                        <MarkdownText text={results.understand} />
                                    </div>
                                </div>
                            </div>

                            {/* Result: Image */}
                            <div className="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden flex flex-col h-fit sticky top-6">
                                <div className="p-4 bg-gray-50 border-b border-gray-100 flex items-center gap-2 text-purple-600 font-bold">
                                    <Icons.Image /> AI Generated Concept Image
                                </div>
                                <div className="p-6 bg-white flex justify-center items-center">
                                    <img 
                                        src={results.image} 
                                        alt="Concept representation" 
                                        className="w-full rounded-lg shadow-sm border border-gray-200"
                                    />
                                </div>
                                <div className="p-4 bg-gray-50 border-t border-gray-100 text-xs text-gray-500 italic">
                                    Generated using prompt: "{prompts.image}"
                                </div>
                            </div>
                        </div>
                    )}
                </div>
            );

            return (
                <div className="min-h-screen flex flex-col">
                    {/* Header Bar */}
                    <header className="bg-white border-b border-gray-200 px-6 py-4 flex justify-between items-center sticky top-0 z-10">
                        <div className="font-bold text-xl tracking-tight text-slate-800 flex items-center gap-2">
                            <div className="bg-slate-900 p-1.5 rounded-md text-white">
                                <Icons.Settings />
                            </div>
                            AI Topic Explorer
                        </div>
                        <div className="flex items-center gap-2 text-xs font-medium">
                            <span className="relative flex h-3 w-3 mr-1">
                                {aiStatus.includes('Connected') ? (
                                    <><span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span><span className="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span></>
                                ) : (
                                    <span className="relative inline-flex rounded-full h-3 w-3 bg-amber-500"></span>
                                )}
                            </span>
                            <span className="text-gray-600">{aiStatus}</span>
                        </div>
                    </header>

                    {/* Main Content Area */}
                    <main className="flex-1 p-6 md:p-12">
                        {/* Progress Indicator */}
                        <div className="max-w-md mx-auto flex justify-between items-center mb-12 relative">
                            <div className="absolute left-0 top-1/2 -translate-y-1/2 w-full h-1 bg-gray-200 -z-10 rounded-full"></div>
                            <div className={`absolute left-0 top-1/2 -translate-y-1/2 h-1 bg-blue-500 -z-10 rounded-full transition-all duration-500`} style={{ width: step === 1 ? '0%' : step === 2 ? '50%' : '100%' }}></div>
                            
                            {[1, 2, 3].map((num) => (
                                <div key={num} className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm transition-colors ${step >= num ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-500'}`}>
                                    {num}
                                </div>
                            ))}
                        </div>

                        {/* Step Rendering Logic */}
                        {step === 1 && renderStep1()}
                        {step === 2 && renderStep2()}
                        {step === 3 && renderStep3()}
                    </main>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-18T20:24:09+05:30.
</ADDITIONAL_METADATA>